#include "worldModel.h"
#include "client.h"

WorldModelV1::WorldModelV1()
{
    //M_loc_engine = new LocalizationEngine( Client::instance().game_data,
    //                                       Client::instance().command_commited );

}

void WorldModelV1::update( )
{
    /*SensorHandler & sensor_h = Client::instance().game_data->sensor_handler;

    switch (sensor_h.last_sensor_type)
    {
    case SENSOR_SEE:
        break;
    case SENSOR_BODY:
        break;
    }

    //SensorType sensor_type = obs.last_obs_type;
    //switch*/
}
